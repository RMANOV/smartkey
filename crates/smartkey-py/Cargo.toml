[package]
name = "smartkey-py"
version = "0.1.0"
edition = "2021"

[lib]
name = "smartkey_py"
crate-type = ["cdylib"]

[dependencies]
smartkey-core = { path = "../smartkey-core" }
pyo3 = { version = "0.28", features = ["extension-module"] }
